<Page x:Class="Bska.Client.UI.Views.MunitionView.SellerManagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bska.Client.UI.Views.MunitionView"
      mc:Ignorable="d" xmlns:UC="clr-namespace:Bska.Client.UI.UserControlls"
       FontFamily="{StaticResource PrimaryFontFamily}"
	  Title="فروشنده ها" FlowDirection="RightToLeft" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}">
    <Page.InputBindings>
        <KeyBinding Key="F1" Command="{Binding HelpCommand}"/>
        <KeyBinding Key="F3" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="F4" Command="{Binding NewCommand}"/>
    </Page.InputBindings>
    <Grid>
        <UC:SellerUC  x:Name="sellerUc"/>
    </Grid>
</Page>
